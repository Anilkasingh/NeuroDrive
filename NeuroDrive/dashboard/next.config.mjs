
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [new URL('https://ui.shadcn.com/avatars/shadcn.jpg')]
    }
};

export default nextConfig;
